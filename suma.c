#include <stdio.h>

// Agregue la definición de la función sumar
int sumar(int a, int b) {
    return a + b;
}

int main() {
    int resultado = sumar(5, 7);
    printf("La suma es: %d\n", resultado);
    return 0;
}
